pipeline {
    agent any
        stages {
            stage('build') {
                steps {
                    sh 'mvn -v'
                }                 
            }
            stage('test') {
                steps {
                    sh 'mvn -v'
                }                 
            }
            stage('deploy') {
                steps {
                    sh 'mvn -v'
                }                 
            }
        }
}
